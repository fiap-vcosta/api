{
  "name": "demo — erros HTTP na API (OS)",
  "type": "query alert",
  "query": "sum(last_10m):sum:trace.aspnet_core.request.errors{service:api,env:prod}.as_count() > 5",
  "message": "{{#is_alert}}Taxa de erros HTTP da API (`service:api`, `env:prod`) acima do limiar na janela da demo.{{/is_alert}}\n{{#is_recovery}}Erros HTTP da API voltaram ao normal.{{/is_recovery}}",
  "tags": [
    "service:api",
    "env:prod",
    "demo:tech-challenge"
  ],
  "options": {
    "thresholds": {
      "critical": 5
    },
    "notify_no_data": false,
    "require_full_window": false,
    "include_tags": true,
    "new_host_delay": 300,
    "notify_audit": false
  },
  "priority": 3
}
