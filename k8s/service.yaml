apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: techchallenge
spec:
  type: NodePort
  selector:
    app: api
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
